import { TaskAdatMegjelenitesPipe } from './task-adat-megjelenites.pipe';

describe('TaskAdatMegjelenitesPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskAdatMegjelenitesPipe();
    expect(pipe).toBeTruthy();
  });
});
