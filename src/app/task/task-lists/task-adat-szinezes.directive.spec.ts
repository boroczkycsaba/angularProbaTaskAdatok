import { TaskAdatSzinezesDirective } from './task-adat-szinezes.directive';

describe('TaskAdatSzinezesDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskAdatSzinezesDirective();
    expect(directive).toBeTruthy();
  });
});
